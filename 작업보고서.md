# 작업 보고서: SHA-2 / SHA-3 해시 함수 추가

**작업 일시**: 2026-06-02  
**이전 작업**: AES 6개 운영 모드  
**이번 작업**: SHA-2(224/256/384/512) + SHA-3(224/256/384/512) 구현  
**결과**: ✅ 성공 (테스트 74/74 통과) / GitHub push 완료

---

## 1. 추가된 모듈

| 파일 | 내용 |
|------|------|
| `src/sha2.rs` | SHA-224, SHA-256, SHA-384, SHA-512 |
| `src/sha3.rs` | SHA3-224, SHA3-256, SHA3-384, SHA3-512 |

---

## 2. SHA-2 구현 (NIST FIPS 180-4)

### 구조
SHA-256/224와 SHA-512/384는 같은 압축 함수를 공유하고 초기값(H0)과 출력 길이만 다릅니다.

| 변형 | 블록 크기 | 워드 크기 | 라운드 | 출력 |
|------|-----------|-----------|--------|------|
| SHA-224 | 512비트 | 32비트 | 64 | 224비트 |
| SHA-256 | 512비트 | 32비트 | 64 | 256비트 |
| SHA-384 | 1024비트 | 64비트 | 80 | 384비트 |
| SHA-512 | 1024비트 | 64비트 | 80 | 512비트 |

### 핵심 연산
```
Ch(x,y,z)  = (x AND y) XOR (NOT x AND z)
Maj(x,y,z) = (x AND y) XOR (x AND z) XOR (y AND z)
Σ0(x) = ROTR²(x) XOR ROTR¹³(x) XOR ROTR²²(x)   [SHA-256]
Σ1(x) = ROTR⁶(x) XOR ROTR¹¹(x) XOR ROTR²⁵(x)   [SHA-256]
```

### 패딩
```
메시지 || 0x80 || 0x00...0x00 || len(메시지)(64비트 big-endian)
→ 512비트(SHA-256) 또는 1024비트(SHA-512) 배수
```

---

## 3. SHA-3 구현 (NIST FIPS 202)

### Keccak 스폰지 구조
SHA-3는 Keccak-p[1600] 치환 함수 기반의 스폰지 구조입니다.
- **State**: 5×5×64비트 = 1600비트
- **흡수(Absorb)**: rate 바이트씩 State에 XOR → Keccak-f 적용
- **압착(Squeeze)**: State에서 출력 길이만큼 추출

| 변형 | Rate | Capacity | 출력 |
|------|------|----------|------|
| SHA3-224 | 1152비트 | 448비트 | 224비트 |
| SHA3-256 | 1088비트 | 512비트 | 256비트 |
| SHA3-384 | 832비트 | 768비트 | 384비트 |
| SHA3-512 | 576비트 | 1024비트 | 512비트 |

### Keccak-f 5단계 치환
```
θ (Theta)  : 열 패리티 XOR → 확산
ρ (Rho)    : 레인별 비트 회전
π (Pi)     : 레인 위치 재배치
χ (Chi)    : 행 단위 비선형 변환 (유일한 비선형 단계)
ι (Iota)   : 라운드 상수 XOR (대칭성 파괴)
```

### SHA-3 도메인 분리
- SHA-3 패딩: `0x06` (Keccak 원본 `0x01`과 구분)
- 마지막 rate 블록 끝에 `0x80` OR

---

## 4. 테스트 결과

```
test result: ok. 74 passed; 0 failed
```

### 신규 테스트 (23개)

| 모듈 | 테스트 | 벡터 출처 |
|------|--------|-----------|
| SHA-256 | 빈 문자열, "abc", 긴 문자열, 100만 'a' | NIST FIPS 180-4 |
| SHA-224 | 빈 문자열, "abc", 긴 문자열 | NIST FIPS 180-4 |
| SHA-512 | 빈 문자열, "abc", 긴 문자열 | NIST FIPS 180-4 |
| SHA-384 | 빈 문자열, "abc", 긴 문자열 | NIST FIPS 180-4 |
| SHA3-256 | 빈 문자열, "abc", 긴 문자열 | NIST FIPS 202 |
| SHA3-224 | 빈 문자열, "abc" | NIST FIPS 202 |
| SHA3-384 | 빈 문자열, "abc" | NIST FIPS 202 |
| SHA3-512 | 빈 문자열, "abc", 긴 문자열 | NIST FIPS 202 |

> 참고: 테스트 작성 시 일부 벡터에 오타가 있었으나, Python hashlib으로 정확한 값을 검증 후 수정 완료.  
> 구현 자체는 처음부터 정확했습니다.

---

## 5. 전체 커밋 히스토리

```
cc34e2d feat: add SHA-2 and SHA-3 hash functions
00238a3 docs: update work report for v2 (6 modes + GitHub setup)
2263b1b feat: initial AES implementation with 6 operation modes
```

GitHub: https://github.com/cbh0819/rust_crypto_service

---

## 6. 현재 전체 라이브러리 현황

| 기능 | 상태 |
|------|------|
| AES-128/192/256 블록 암호 | ✅ |
| ECB, CBC, CFB8, CFB128, OFB, CTR | ✅ |
| PKCS#7 패딩 | ✅ |
| SHA-224, SHA-256, SHA-384, SHA-512 | ✅ |
| SHA3-224, SHA3-256, SHA3-384, SHA3-512 | ✅ |
| HMAC | 🔜 |
| ChaCha20 | 🔜 |
| RSA / ECC | 🔜 |

---

## 7. 향후 작업 후보

- **HMAC**: SHA-2/SHA-3 기반 메시지 인증
- **PBKDF2**: 키 유도 함수
- **ChaCha20-Poly1305**: 현대적 AEAD
