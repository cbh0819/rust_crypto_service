# 작업 보고서: AES 운영 모드 확장 + GitHub 준비

**작업 일시**: 2026-06-02  
**이전 작업**: AES 코어 구현 (ECB, CBC)  
**이번 작업**: CFB8, CFB128, OFB, CTR 추가 + Git 구조 완성  
**결과**: ✅ 성공 (테스트 51/51 통과)

---

## 1. 이번 작업 변경 사항

### 추가된 운영 모드 (4개)

| 모드 | 세그먼트 | 패딩 | 핵심 특징 |
|------|----------|------|-----------|
| CFB8 | 8비트 (1바이트) | 없음 | Shift register, 바이트 스트림 |
| CFB128 | 128비트 (16바이트) | 없음 | 블록 단위 CFB, 부분 블록 처리 |
| OFB | 128비트 | 없음 | 키스트림 독립 생성, 오류 비전파 |
| CTR | 128비트 | 없음 | Nonce+Counter, 병렬/랜덤 접근 |

### 추가된 파일
- `README.md` — 프로젝트 문서
- `.gitignore` — Rust 빌드 파일 제외

### 추가된 편의 함수 (lib.rs)
- `aes128_cfb8_encrypt/decrypt`
- `aes128_cfb128_encrypt/decrypt`
- `aes128_ofb_encrypt/decrypt`
- `aes128_ctr_encrypt/decrypt`
- `aes256_ctr_encrypt/decrypt`

---

## 2. 전체 프로젝트 구조

```
rust_aes_lib/
├── .gitignore
├── Cargo.toml
├── README.md
└── src/
    ├── lib.rs           # 공개 API + 통합 테스트 (3개)
    ├── constants.rs     # S-Box, 역 S-Box, RCON
    ├── gf.rs            # GF(2⁸) 연산 (5개 테스트)
    ├── key_schedule.rs  # 키 확장 AES-128/192/256 (5개 테스트)
    ├── block.rs         # AES 코어 4×4 State 연산 (10개 테스트)
    ├── padding.rs       # PKCS#7 (7개 테스트)
    └── modes.rs         # ECB/CBC/CFB8/CFB128/OFB/CTR (21개 테스트)
```

---

## 3. 운영 모드 구현 상세

### CFB8
```
암호화: CT[i] = PT[i] XOR AES(ShiftReg)[0]
        ShiftReg = ShiftReg << 1 || CT[i]
복호화: PT[i] = CT[i] XOR AES(ShiftReg)[0]  (동일 구조)
```

### CFB128
```
암호화: CT[i..i+16] = PT[i..i+16] XOR AES(ShiftReg)
        ShiftReg = CT[i..i+16]
복호화: PT[i..i+16] = CT[i..i+16] XOR AES(ShiftReg)  (동일 구조)
```

### OFB
```
KeyStream 생성: O[0] = IV
                O[i] = AES(O[i-1])   ← 평문/암호문 무관
암호화/복호화:  result = data XOR O[i]  (완전히 동일한 연산)
```

### CTR
```
Counter Block: Nonce(12바이트) || Counter(4바이트, big-endian)
KeyStream:     KS[i] = AES(Nonce || i)
암호화/복호화: result[i] = data[i] XOR KS[i]  (동일한 연산)
랜덤 접근:     블록 i만 ctr_process(data, ..., start_counter=i) 로 독립 처리 가능
```

---

## 4. 테스트 결과

```
test result: ok. 51 passed; 0 failed
```

### NIST 테스트 벡터 통과 목록

| 표준 | 항목 |
|------|------|
| NIST FIPS 197 부록 A.1 | AES-128 키 스케줄 |
| NIST FIPS 197 부록 B | AES-128 전체 암/복호화 |
| NIST FIPS 197 부록 B | AES-256 암호화 |
| NIST SP 800-38A | ECB-AES128 블록 1 |
| NIST SP 800-38A | CBC-AES128 블록 1 |
| NIST SP 800-38A | CFB8-AES128 블록 1~2 |
| NIST SP 800-38A | CFB128-AES128 블록 1 |
| NIST SP 800-38A | OFB-AES128 블록 1 |

### 추가 검증 항목

| 테스트 | 모드 |
|--------|------|
| 오류 비전파 (1비트 오류 → 1비트만 영향) | OFB |
| 랜덤 접근 (블록 2만 독립 복호화) | CTR |
| 부분 블록 처리 (패딩 없이 임의 길이) | CFB8/CFB128/OFB/CTR |
| 다른 Nonce → 다른 암호문 | CTR |
| 암호화 = 복호화 연산 동일성 | OFB, CTR |

---

## 5. GitHub Push 방법

저장소가 로컬에 커밋까지 완료된 상태입니다.  
아래 명령어로 기존 저장소에 push하면 됩니다.

```bash
# 1. 원격 저장소 등록 (한 번만)
git remote add origin https://github.com/<username>/<repo-name>.git

# 2. Push
git push -u origin master

# 또는 main 브랜치를 사용하는 경우
git branch -M main
git push -u origin main
```

**커밋 내역**
```
2263b1b feat: initial AES implementation with 6 operation modes
```

---

## 6. 향후 확장 가능 항목

- **추가 알고리즘**: ChaCha20, SHA-256, HMAC, PBKDF2
- **GCM 모드**: CTR + GHASH 인증 (AEAD)
- **최적화**: T-table 기반 MixColumns
- **사이드 채널 대응**: 상수 시간 구현
- **벤치마크**: `criterion` crate 연동
