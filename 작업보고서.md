# 작업 보고서: Rust AES 암호화 라이브러리

**작업 일시**: 2026-06-02  
**목적**: 학습용 순수 Rust AES 암호 라이브러리 구현  
**결과**: ✅ 성공 (테스트 41/41 통과)

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 라이브러리 이름 | `rust_aes_lib` |
| 언어 | Rust (Edition 2021) |
| 외부 의존성 | **없음** (순수 Rust 구현) |
| 지원 키 크기 | AES-128 / AES-192 / AES-256 |
| 운영 모드 | ECB, CBC |
| 패딩 방식 | PKCS#7 |
| 표준 준수 | NIST FIPS 197, NIST SP 800-38A |

---

## 2. 구현 파일 구조

```
rust_aes_lib/
├── Cargo.toml
└── src/
    ├── lib.rs           # 공개 API 진입점 + 통합 테스트
    ├── constants.rs     # S-Box, 역 S-Box, RCON 테이블
    ├── gf.rs            # GF(2^8) 갈루아 필드 연산
    ├── key_schedule.rs  # AES 키 스케줄 (키 확장)
    ├── block.rs         # AES 코어 블록 암/복호화
    ├── padding.rs       # PKCS#7 패딩
    └── modes.rs         # 운영 모드 (ECB, CBC)
```

---

## 3. 모듈별 구현 내용

### 3.1 `constants.rs` — 상수 테이블
- **SBOX** (256바이트): GF(2^8) 곱셈 역원 + 아핀 변환
- **INV_SBOX** (256바이트): SubBytes의 역 치환 테이블
- **RCON** (11바이트): 키 스케줄용 라운드 상수

### 3.2 `gf.rs` — 갈루아 필드 연산
- `gf_mul(a, b)`: GF(2^8) 위의 곱셈 (러시아 농부 알고리즘)
  - 기약 다항식: x⁸ + x⁴ + x³ + x + 1 (0x1b)
- `xtime(a)`: x 곱셈 (MixColumns 최적화용)

### 3.3 `key_schedule.rs` — 키 확장
- `KeySize` 열거형: Aes128 / Aes192 / Aes256
- `key_expansion()`: NIST 명세에 따른 키 확장
  - AES-128: 16바이트 → 176바이트 (11 라운드 키)
  - AES-192: 24바이트 → 208바이트 (13 라운드 키)
  - AES-256: 32바이트 → 240바이트 (15 라운드 키)
- `rot_word()`, `sub_word()`: 키 스케줄 내부 함수

### 3.4 `block.rs` — AES 코어 연산
AES 4x4 State 행렬 기반 전체 구현:

| 함수 | 설명 |
|------|------|
| `bytes_to_state()` | 바이트 배열 → 4×4 State (열 우선) |
| `state_to_bytes()` | State → 바이트 배열 |
| `sub_bytes()` | S-Box 바이트 치환 |
| `inv_sub_bytes()` | 역 S-Box 바이트 치환 |
| `shift_rows()` | 행 왼쪽 회전 (0/1/2/3칸) |
| `inv_shift_rows()` | 행 오른쪽 회전 |
| `mix_columns()` | GF(2^8) 행렬 곱셈 |
| `inv_mix_columns()` | 역 행렬 곱셈 |
| `add_round_key()` | 라운드 키 XOR |
| `aes_encrypt_block()` | 16바이트 블록 암호화 |
| `aes_decrypt_block()` | 16바이트 블록 복호화 |

### 3.5 `padding.rs` — PKCS#7 패딩
- `pad()`: 블록 크기(16바이트)에 맞게 패딩 추가
- `unpad()`: 패딩 검증 및 제거 (잘못된 패딩 시 `Err` 반환)

### 3.6 `modes.rs` — 운영 모드

#### ECB (Electronic Codebook)
- 각 블록을 독립적으로 암호화
- ⚠️ 학습 목적 전용 — 동일 평문 → 동일 암호문 (패턴 노출)

#### CBC (Cipher Block Chaining)
- 이전 암호문 블록과 XOR 후 암호화
- IV(초기화 벡터) 필요 — 매 암호화마다 랜덤 값 사용 권장

---

## 4. 테스트 결과

```
test result: ok. 41 passed; 0 failed; 0 ignored
```

### 모듈별 테스트 분포

| 모듈 | 테스트 수 | 내용 |
|------|-----------|------|
| `gf` | 5 | 곱셈 항등원, 교환 법칙, FIPS 197 예시 |
| `key_schedule` | 5 | 키 길이, NIST 라운드 키 검증 |
| `block` | 10 | NIST FIPS 197 암/복호화 벡터, 역연산 항등성 |
| `padding` | 7 | 패딩 추가/제거, 라운드트립, 오류 처리 |
| `modes` | 10 | ECB/CBC NIST SP 800-38A 벡터, 라운드트립 |
| `통합 테스트` | 4 | 빈 메시지, 1KB 대용량, AES-128/256 파이프라인 |

### 주요 NIST 테스트 벡터 검증 항목
- ✅ NIST FIPS 197 부록 A.1 — AES-128 키 스케줄
- ✅ NIST FIPS 197 부록 B — AES-128 전체 암/복호화
- ✅ NIST FIPS 197 부록 B — AES-256 암호화
- ✅ NIST SP 800-38A — ECB 모드 테스트 벡터
- ✅ NIST SP 800-38A — CBC 모드 테스트 벡터

---

## 5. 공개 API

```rust
// 키 크기
pub enum KeySize { Aes128, Aes192, Aes256 }

// 저수준 블록 암/복호화 (16바이트)
pub fn aes_encrypt_block(plaintext: &[u8; 16], key: &[u8], key_size: KeySize) -> [u8; 16]
pub fn aes_decrypt_block(ciphertext: &[u8; 16], key: &[u8], key_size: KeySize) -> [u8; 16]

// 운영 모드 (임의 길이, PKCS#7 패딩 자동 처리)
pub fn ecb_encrypt(plaintext: &[u8], key: &[u8], key_size: KeySize) -> Vec<u8>
pub fn ecb_decrypt(ciphertext: &[u8], key: &[u8], key_size: KeySize) -> Result<Vec<u8>, &'static str>
pub fn cbc_encrypt(plaintext: &[u8], key: &[u8], key_size: KeySize, iv: &[u8; 16]) -> Vec<u8>
pub fn cbc_decrypt(ciphertext: &[u8], key: &[u8], key_size: KeySize, iv: &[u8; 16]) -> Result<Vec<u8>, &'static str>

// 편의 함수
pub fn aes128_cbc_encrypt(plaintext: &[u8], key: &[u8; 16], iv: &[u8; 16]) -> Vec<u8>
pub fn aes128_cbc_decrypt(ciphertext: &[u8], key: &[u8; 16], iv: &[u8; 16]) -> Result<Vec<u8>, &'static str>
pub fn aes256_cbc_encrypt(plaintext: &[u8], key: &[u8; 32], iv: &[u8; 16]) -> Vec<u8>
pub fn aes256_cbc_decrypt(ciphertext: &[u8], key: &[u8; 32], iv: &[u8; 16]) -> Result<Vec<u8>, &'static str>
```

---

## 6. 사용 예시

```rust
use rust_aes_lib::{aes128_cbc_encrypt, aes128_cbc_decrypt};

let key = [0x2b, 0x7e, 0x15, 0x16, 0x28, 0xae, 0xd2, 0xa6,
           0xab, 0xf7, 0x15, 0x88, 0x09, 0xcf, 0x4f, 0x3c];
let iv  = [0x00u8; 16]; // 실제 사용 시 CSPRNG으로 생성

let plaintext = b"Hello, AES!";
let ciphertext = aes128_cbc_encrypt(plaintext, &key, &iv);
let recovered  = aes128_cbc_decrypt(&ciphertext, &key, &iv).unwrap();

assert_eq!(recovered, plaintext);
```

---

## 7. 학습 포인트 정리

| 개념 | 구현 위치 | 핵심 내용 |
|------|-----------|-----------|
| GF(2^8) 연산 | `gf.rs` | 기약 다항식을 이용한 유한체 곱셈 |
| S-Box 원리 | `constants.rs` | 곱셈 역원 + 아핀 변환으로 생성 |
| 키 스케줄 | `key_schedule.rs` | RotWord → SubWord → XOR RCON |
| ShiftRows | `block.rs` | 행 단위 순환 시프트로 확산 제공 |
| MixColumns | `block.rs` | GF(2^8) 행렬 곱셈으로 혼합 제공 |
| CBC 체이닝 | `modes.rs` | 이전 암호문 블록 XOR로 의존성 형성 |

---

## 8. 향후 확장 가능 항목

- **추가 운영 모드**: CTR, GCM (인증 암호화), CFB, OFB
- **추가 알고리즘**: ChaCha20, SHA-256, HMAC
- **최적화**: Look-up 테이블 기반 MixColumns (T-table)
- **사이드 채널 대응**: 상수 시간 구현 (constant-time)
- **벤치마크**: `criterion` 크레이트 활용 성능 측정

---

## 9. 보안 주의사항

> ⚠️ 이 라이브러리는 **학습 목적 전용**입니다.
> 
> - 사이드 채널 공격(타이밍, 캐시) 대응 미구현
> - 보안 검증(audit) 미수행
> - **프로덕션 환경에서는 `ring`, `aes`, `openssl` 등 검증된 라이브러리 사용 권장**
